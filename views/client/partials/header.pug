div(class="bg-black") 
  div(class="container mx-auto")
    div(class="wrapper py-[20px] flex text-[24px] justify-between  text-white")
      h1(claas="") 
        a(href="/home") ATSH Tracker
      ul(class="flex gap-4")
        if(!user)
          li
          a(href="/user/login") Login
          li 
            a(href="/user/register") Register
        else
          li
            a(href="/user/logout") Logout
          li
            a(href="/home/upload") Upload
